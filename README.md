# reu20024project
Repository for PROTECT REU 2024 Project on LLMs

In order to build this project, simply run `npm run build`, which will install required node packages, as well as create a venv for needed python libraries. The front end can then be launched using `npm run start`